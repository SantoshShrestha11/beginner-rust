use gui_thing::Draw;
struct SelectBox {
    width: u32,
    height: u32,
    options: Vec<String>,
}
impl Draw for SelectBox {
    fn draw(&self) {
        //code to acutally draw a select box goes here
    }
}
fn main() {
}
