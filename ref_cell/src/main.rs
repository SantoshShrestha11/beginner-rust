fn main() {
    // let a: i32 = 5;
    // // here a is not mutable so we can take a reference 
    // let b: &mut i32 =  &mut a;
    //
    // let mut c: i32 = 10;
    // // here c is mutable but hte  d is not so we can't take a reference and change it's value 
    // let d:&i32 = &c;
    // // here the reference self is non mutable  
    // *d = 20;

    let c: i32 = 6;
    let d: &i32 = &c;
}
